<% content_for(:title, t('.title')) %>
<div class="container mx-auto p-6 font-mono relative">
  <%= render 'search', search_review: @search_user %>
  <div class="w-full mb-8 overflow-hidden rounded-lg shadow-lg">
    <div class="w-full overflow-x-auto">
      <table class="w-full">
        <thead>
          <tr class="text-md font-semibold tracking-wide text-left text-gray-900 bg-gray-100 uppercase border-gray-600">
            <th class="px-4 py-3"><%= User.human_attribute_name(:id) %></th>
            <th class="px-4 py-3"><%= User.human_attribute_name(:name) %></th>
            <th class="px-4 py-3"><%= User.human_attribute_name(:role) %></th>
            <th class="px-4 py-3"><%= User.human_attribute_name(:created_at) %></th>
            <th class="px-4 py-3"></th>
          </tr>
        </thead>
        <tbody class="bg-white">
          <%= render partial: 'user', collection: @users %>
        </tbody>
      </table>
    </div>
  </div>
  <div class="absolute flex justify-center inset-x-0 bottom-0">
    <%= paginate @users %>
  </div>
</div>
